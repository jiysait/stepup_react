import React from 'react'
import Destructuring from './Destructuring'

export default function Main() {
    /**
     * Use custom components here by importing them
     * 
     * 1. lifecycle
     * 2. async (setState)
     * 
     */
    return(
        <Destructuring />
    )
}